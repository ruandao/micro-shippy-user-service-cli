#!/bin/bash
#
#echo "try to ping ${SER_HOST}: ${SER_PORT}";
#while ! nc -z "${SER_HOST}" "${SER_PORT}"; do
#  echo "try to ping ${SER_HOST}: ${SER_PORT} fail";
#  sleep 3;
#done

./shippy-cli-create-user